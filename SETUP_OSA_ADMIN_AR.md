# 🔐 إعداد حساب OSA الإداري - BUA Asset Management

## المقدمة

تم إعداد نظام إدارة الأصول بـ BUA مع حساب إداري كامل الصلاحيات لـ **OSA** (محمد اسامه) مع القدرة على إضافة حسابات جديدة.

---

## ✅ بيانات الدخول - Login Credentials

### حساب المسؤول الأساسي (OSA)
```
اسم المستخدم (Username): Osa
كلمة المرور (Password):    123
الاسم:                      محمد اسامه
البريد الإلكتروني:         osa@bua.edu.eg
الصلاحيات:                  Admin (صلاحيات كاملة)
```

### حساب النظام الاحتياطي (System Admin)
```
اسم المستخدم (Username): admin
كلمة المرور (Password):    admin123
الاسم:                      نظام الإدارة
البريد الإلكتروني:         admin@bua.edu.eg
الصلاحيات:                  Admin (صلاحيات كاملة)
```

---

## 🚀 خطوات التشغيل الأولية

### 1️⃣ تشغيل سكريبت الإعداد

```bash
# من مجلد المشروع الجذر
npm run setup-osa

# أو باستخدام pnpm إن كنت تستخدمه
pnpm setup-osa

# أو مباشرة مع tsx/ts-node
npx tsx setup-osa-admin.ts
```

### 2️⃣ التحقق من النجاح

بعد تشغيل السكريبت، يجب أن ترى رسالة نجاح تحتوي على:
- ✅ معلومات حساب OSA الإداري
- ✅ معلومات الحسابات المُنشأة
- ✅ قائمة بجميع المستخدمين في النظام

### 3️⃣ الدخول للنظام

1. افتح تطبيق BUA Asset Management
2. في صفحة تسجيل الدخول، ستجد بيانات حساب OSA معروضة
3. أدخل `Osa` و `123`
4. انقر على "Sign In"

---

## 👥 إضافة حسابات جديدة

### الطريقة الأولى: من واجهة المستخدم (الموصى به)

1. **سجل الدخول** باستخدام حساب OSA
2. **اذهب إلى**: القائمة الجانبية → Users / إدارة المستخدمين
3. **انقر على**: "Add User" / "إضافة مستخدم جديد"
4. **ملء البيانات**:
   - البريد الإلكتروني (Email)
   - الاسم الكامل (Full Name)
   - الدور/الصلاحيات (Role)
5. **انقر**: "Create" / "إنشاء"

### الطريقة الثانية: عبر API (للمطورين)

```bash
curl -X POST http://localhost:5000/api/trpc/auth.createUser \
  -H "Content-Type: application/json" \
  -H "Cookie: auth-session=YOUR_SESSION" \
  -d '{
    "json": {
      "email": "user@example.com",
      "name": "اسم المستخدم",
      "role": "unit_manager"
    }
  }'
```

### الطريقة الثالثة: مباشرة من قاعدة البيانات

```sql
INSERT INTO users (openId, name, email, loginMethod, role, createdAt, updatedAt, lastSignedIn)
VALUES (
  'local-unique-id',
  'اسم المستخدم',
  'email@example.com',
  'local',
  'unit_manager',
  NOW(),
  NOW(),
  NOW()
);
```

---

## 🎯 الأدوار والصلاحيات

### 1. Admin (الإداري) 👨‍💼
**الصلاحيات الكاملة:**
- ✅ إنشاء وتعديل وحذف الأجهزة
- ✅ إدارة التحويلات والصيانة
- ✅ إضافة وتعديل المستخدمين
- ✅ تغيير أدوار المستخدمين
- ✅ عرض سجلات الفحص الكاملة
- ✅ استيراد وتصدير البيانات
- ✅ عرض التحليلات الكاملة

### 2. Unit Manager (مدير الوحدة) 🏢
**صلاحيات محدودة بالفاكولتي/الكلية:**
- ✅ إدارة أجهزة الفاكولتي الخاصة به
- ✅ الموافقة على التحويلات والصيانة
- ✅ عرض التحليلات الخاصة بالفاكولتي
- ❌ لا يمكن إضافة مستخدمين جدد

### 3. Technician (فني) 🔧
**صلاحيات محدودة للصيانة:**
- ✅ عرض قائمة الأجهزة
- ✅ إكمال طلبات الصيانة المعينة
- ❌ لا يمكن إنشاء أجهزة جديدة
- ❌ لا يمكن إدارة التحويلات

### 4. User (مستخدم عام) 👤
**صلاحيات عرض فقط:**
- ✅ مسح QR codes للأجهزة
- ✅ عرض معلومات الجهاز العام
- ❌ لا يمكن الوصول للإدارة

---

## 📝 البيانات المطلوبة عند إنشاء مستخدم جديد

```json
{
  "email": "user@bua.edu.eg",           // البريد الإلكتروني الفريد
  "name": "اسم المستخدم",              // الاسم الكامل
  "role": "unit_manager"                // admin | unit_manager | technician | user
}
```

---

## 🔧 معالجة المشاكل

### المشكلة: لا يعمل تسجيل الدخول

**الحل:**
1. تأكد من تشغيل السكريبت `npm run setup-osa`
2. تحقق من اتصال قاعدة البيانات
3. تأكد من صحة بيانات الدخول (Osa/123)

### المشكلة: لا أستطيع إضافة مستخدمين جدد

**الحل:**
1. تأكد من أنك مسجل دخول بحساب Admin
2. اذهب إلى Users من القائمة الجانبية
3. تحقق من أن صلاحيتك هي "admin" (تحقق من الملف الشخصي)

### المشكلة: نسيت بيانات دخول الحساب

**الحل:**
1. قم بتشغيل السكريبت مرة أخرى: `npm run setup-osa`
2. سيتم تحديث حسابات OSA و System Admin بالبيانات الصحيحة

---

## 📊 عرض جميع المستخدمين

للتحقق من جميع المستخدمين المُنشأين:

```bash
# من النظام
npm run list-users

# أو مباشرة من قاعدة البيانات
mysql> SELECT id, name, email, role, createdAt FROM users ORDER BY id;
```

---

## 🔐 نصائح الأمان

1. **غيّر كلمات المرور الافتراضية** بعد التثبيت الأول
2. **لا تشارك بيانات الدخول** إلا مع المسؤولين
3. **استخدم بريد إلكتروني فريد** لكل حساب
4. **راجع سجلات الفحص** بانتظام للكشف عن النشاط المريب
5. **احذر من الحسابات المفعلة غير الضرورية**

---

## 📚 روابط مهمة

- **داشبورد**: `/dashboard`
- **إدارة المستخدمين**: `/users`
- **إدارة الأجهزة**: `/devices`
- **سجل الفحص**: `/audit`
- **الإعدادات**: `/settings` (إن وُجدت)

---

## ✨ ملاحظات مهمة

- تم تفعيل إعدادات OSA الكاملة تلقائياً عند تشغيل السكريبت
- يمكنك إضافة عدد غير محدود من المستخدمين الجدد
- كل حساب له openId فريد لتتبعه
- جميع العمليات تُسجل في جدول الفحص (Audit Log)

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من سجلات التطبيق (Console)
2. تأكد من توفر قاعدة البيانات
3. اتصل بفريق الدعم الفني

---

**آخر تحديث**: January 31, 2026
**الإصدار**: 1.0
**الحالة**: ✅ جاهز للاستخدام
