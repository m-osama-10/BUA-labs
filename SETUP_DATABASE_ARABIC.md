# 🛠️ طريقة إنشاء قاعدة البيانات - خطوة بخطوة

## الطريقة الأولى: استخدام MySQL Workbench (الأسهل والأسرع) ✅

### المتطلبات:
- MySQL Workbench مثبت على جهازك
- MySQL Server يعمل بشكل صحيح

### الخطوات:

#### 1️⃣ افتح MySQL Workbench
```
ابدأ (Start) → MySQL Workbench
```

#### 2️⃣ اتصل بـ MySQL
```
في الصفحة الرئيسية، انقر على الاتصال الموجود (عادة يكون localhost:3306)
أو اضغط: File → New Query Tab
```

#### 3️⃣ افتح ملف البيانات
```
في المجلد الخاص بالمشروع:
d:\apps\Web Development\bua-asset-management (2)\database-full-setup.sql
```

#### 4️⃣ انسخ محتوى الملف
```
افتح الملف بـ VS Code أو أي محرر نصوص
اضغط Ctrl+A لتحديد الكل
اضغط Ctrl+C للنسخ
```

#### 5️⃣ الصق في MySQL Workbench
```
في نافذة Query في MySQL Workbench
اضغط Ctrl+V للصق
```

#### 6️⃣ نفذ الأوامر
```
اضغط Ctrl+Shift+Enter (أو اضغط البرق ⚡ في الأعلى)
```

#### 7️⃣ تحقق من النجاح
```
يجب أن تري هذا في الأسفل:
✅ Faculties | Count: 8
✅ Departments | Count: 20
✅ Laboratories | Count: 21
🎉 تم إنشاء قاعدة البيانات بنجاح!
```

---

## الطريقة الثانية: استخدام Command Line (لو فاشل Workbench)

### من Command Prompt أو PowerShell:

```powershell
# انتقل إلى مجلد المشروع
cd "d:\apps\Web Development\bua-asset-management (2)"

# نفذ الملف
mysql -u root -p < database-full-setup.sql
```

إذا طلب منك كلمة المرور، اضغط Enter (إذا لم تكن هناك كلمة مرور)

---

## الطريقة الثالثة: استخدام npm Script (إذا كانت المشكلة مع npm)

```powershell
# تأكد أن MySQL يعمل أولاً!
# ثم:
npm run db:create
```

---

## ✅ بعد إنشاء قاعدة البيانات مباشرة:

### 1. تأكد من وجود .env
```
انسخ .env.example إلى .env
أو أنشئ ملف جديد باسم .env بهذا المحتوى:

DATABASE_URL="mysql://root:@localhost:3306/bua_assets"
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bua_assets
```

### 2. شغل المشروع
```powershell
npm run dev
```

### 3. اختبر في المتصفح
```
اذهب إلى: http://localhost:3000/devices/new
يجب أن ترى 8 كليات في القائمة المنسدلة ✅
```

---

## 🚨 استكشاف الأخطاء والمشاكل

### المشكلة: "Access denied for user 'root'@'localhost'"
**الحل**: قد تحتاج كلمة مرور
```
عدّل في .env:
DB_PASSWORD=YOUR_PASSWORD
```

### المشكلة: "Can't connect to MySQL server"
**الحل**: MySQL غير مشغل
```powershell
# افتح Services (Services.msc)
# ابحث عن MySQL80 أو MySQL
# انقر كليك يمين → Start
```

### المشكلة: "Database exists" أو "Table already exists"
**الحل**: آمن تماماً! السكريبت يستخدم IF NOT EXISTS
```
يمكنك تشغيله مرات عديدة، لن تحصل على مشاكل
```

### المشكلة: الأوامر لم تتنفذ كلها
**الحل**: تأكد من تحديد جميع الأسطر:
```
اختر الكل: Ctrl+A
نفذ: Ctrl+Shift+Enter
```

---

## 📝 ملفات مهمة

| الملف | الوصف |
|------|-------|
| `database-full-setup.sql` | ملف SQL كامل لإنشاء قاعدة البيانات |
| `.env` | ملف الإعدادات (انسخ من .env.example) |
| `drizzle/schema.ts` | تعريف قاعدة البيانات |
| `package.json` | يحتوي على npm scripts |

---

## ❓ أسئلة شائعة

**س: هل سيحذف ملفاتي الأخرى؟**
ج: لا، لن يحذف شيء. قاعدة البيانات منفصلة تماماً.

**س: كم مرة يمكن تشغيل السكريبت؟**
ج: كم تريد! لن يسبب مشاكل.

**س: ماذا إذا حدث خطأ في الوسط؟**
ج: لا تقلق، يمكنك تشغيله مرة أخرى من البداية.

**س: كيفية التحقق من البيانات بعد الإدراج؟**
ج: في MySQL Workbench:
```sql
SELECT COUNT(*) FROM bua_assets.faculties;
SELECT COUNT(*) FROM bua_assets.departments;
SELECT COUNT(*) FROM bua_assets.laboratories;
```

---

## 🎯 الخطوة التالية بعد النجاح

بعد إنشاء قاعدة البيانات بنجاح:

1. تشغيل التطبيق: `npm run dev`
2. فتح الرابط: `http://localhost:3000/devices/new`
3. يجب أن تري الـ 8 كليات الآن ✅

---

**🚀 نجحت! قاعدة بياناتك جاهزة الآن!**
