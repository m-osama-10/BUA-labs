# 🚀 إنشاء قاعدة البيانات والبيانات الأولية

## ✅ الخطوة الوحيدة:

```bash
npm run db:create
```

هذا الأمر سيقوم بـ:
- ✅ الاتصال بـ MySQL
- ✅ إنشاء قاعدة البيانات `bua_assets`
- ✅ إدراج الـ 8 كليات
- ✅ إدراج 20 قسم
- ✅ إدراج 21 مختبر

---

## 📋 المتطلبات:

1. **MySQL مثبت وشغال**
   - تأكد من تشغيل خدمة MySQL80

2. **قيم الاتصال في `.env`:**
   ```
   DB_HOST=localhost
   DB_PORT=3306
   DB_USER=root
   DB_PASSWORD=          # (اتركها فارغة إذا لم تكن هناك كلمة مرور)
   DB_NAME=bua_assets
   ```

---

## 🎯 التشغيل:

```bash
# في مجلد المشروع:
npm run db:create
```

---

## ✨ النتيجة المتوقعة:

```
✅ الاتصال بـ MySQL بنجاح
✅ قاعدة البيانات - تم بنجاح
✅ الكليات - تم بنجاح
✅ الأقسام - تم بنجاح
✅ المختبرات - تم بنجاح

📊 الكليات: 8
📊 الأقسام: 20
📊 المختبرات: 21

🎉 تم إنشاء قاعدة البيانات بنجاح!
```

---

## 📝 الخطوات التالية:

```bash
# 1. شغّل التطبيق:
npm run dev

# 2. افتح المتصفح:
http://localhost:3000/devices/new

# 3. ستظهر الـ 8 كليات! 🎉
```

---

## 🛠️ استكشاف الأخطاء:

### المشكلة: "Can't connect to MySQL"
```
❌ MySQL لا يعمل
✅ الحل: شغّل خدمة MySQL80 من Services
```

### المشكلة: "Access denied"
```
❌ كلمة المرور خاطئة
✅ الحل: عدّل DB_PASSWORD في .env
```

### المشكلة: "Unknown database"
```
❌ قاعدة البيانات موجودة بالفعل
✅ لا مشكلة - السكريبت سيتجاهل الخطأ ويتابع
```

---

**🎊 تم! الآن قاعدة البيانات جاهزة مع البيانات الأولية!**
