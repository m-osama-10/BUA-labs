# 📋 خطوات إضافة الـ 8 كليات

## ✅ الملفات المُعدّة

```
✅ .env                                    - ملف الإعدادات (جاهز)
✅ seed-8-faculties.ts                     - سكريبت البذر
✅ drizzle/0003_insert_8_faculties_data.sql - ملف SQL الكامل
```

## 🚀 الخطوات

### الطريقة 1: باستخدام السكريبت (الأسهل)

```bash
# إذا كان MySQL مشغّل وقاعدة البيانات موجودة:
npm run seed:8faculties
```

### الطريقة 2: تشغيل SQL يدويًا (إذا فشل السكريبت)

افتح MySQL Workbench أو أي أداة MySQL وشغّل:
```
drizzle/0003_insert_8_faculties_data.sql
```

### الطريقة 3: من سطر الأوامر

```bash
mysql -u root bua_assets < drizzle/0003_insert_8_faculties_data.sql
```

## 📊 البيانات التي سيتم إضافتها

### الـ 8 كليات:
```
1. ✅ كلية الصيدلة (PHARM)
2. ✅ كلية طب الفم والأسنان (DENT)
3. ✅ كلية الطب البيطري (VET)
4. ✅ كلية العلاج الطبيعي (PT)
5. ✅ كلية التمريض (NURS)
6. ✅ كلية البيوتكنولوجيا (BIO)
7. ✅ كلية الطب البشري (MED)
8. ✅ كلية العلوم الصحية (HS)
```

### الأقسام والمختبرات:
```
- 20 قسم موزع على الـ 8 كليات
- 21 مختبر مع أسماء عربية وأماكن محددة
```

## ✨ المتوقع بعد الإضافة

في صفحة إضافة جهاز جديد:
```
✅ تظهر قائمة الـ 8 كليات
✅ اختيار كلية → تظهر أقسامها
✅ اختيار قسم → تظهر مختبراته
✅ يمكن حفظ الجهاز بنجاح
```

## 🔍 التحقق من النجاح

```bash
# شغّل أحد هذه الأوامر:
mysql -u root bua_assets -e "SELECT * FROM faculties;"
mysql -u root bua_assets -e "SELECT * FROM departments;"
mysql -u root bua_assets -e "SELECT * FROM laboratories;"
```

## 📞 إذا واجهت مشكلة

**المشكلة:** "Connection refused"
- تأكد من أن MySQL مشغّل
- عدّل قيم الاتصال في `.env`

**المشكلة:** "Unknown database"
- أنشئ قاعدة البيانات يدويًا:
  ```sql
  CREATE DATABASE bua_assets CHARACTER SET utf8mb4;
  ```

**المشكلة:** "Duplicate entry"
- البيانات موجودة بالفعل
- حذفها وأعد التشغيل أو تابع الخطوات التالية

---

✅ **تم! الآن يمكنك إضافة الأجهزة بنجاح!**
