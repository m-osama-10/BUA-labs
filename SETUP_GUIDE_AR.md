# 🔧 إصلاح مشكلة الكليات والأجهزة - دليل الإعداد

## 🔍 المشكلة

عند محاولة إضافة جهاز، لا تظهر الكليات (ال 12 كلية) ولا حتى الأجهزة الأخرى. السبب هو:

1. ❌ **قاعدة البيانات لم تُهيأ** - لا توجد ملف `.env` بقيم الاتصال
2. ❌ **جدول الكليات فارغ** - لم يتم إدراج البيانات الأولية

---

## 📋 خطوات الإصلاح

### الخطوة 1: إنشاء ملف `.env`

إنشئ ملف جديد باسم `.env` في جذر المشروع (بجانب `package.json`) وأضف هذا المحتوى:

```env
# قاعدة البيانات - استخدم أحد الخيارات التالية

# الخيار A: استخدام DATABASE_URL (موصى به)
DATABASE_URL=mysql://user:password@localhost:3306/database_name

# الخيار B: استخدام متغيرات فردية
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_NAME=bua_assets

# مفاتيح أمان (استخدم قيم عشوائية)
JWT_SECRET=your_super_secret_key_here_min_32_chars_long
OWNER_OPEN_ID=default-admin-user

# OAuth Server (حسب نظام المؤسسة)
OAUTH_SERVER_URL=http://your-oauth-server

NODE_ENV=development
```

### الخطوة 2: تحديث قيم الاتصال

استبدل قيم الاتصال بقاعدة البيانات الفعلية:

```
التطوير المحلي:
- DB_HOST: localhost
- DB_USER: root
- DB_PASSWORD: (كلمة مرور MySQL)
- DB_NAME: bua_assets
```

### الخطوة 3: تشغيل سكريبت البذر (Seeding)

بعد تعديل `.env`، شغّل الأمر:

```bash
npm run seed:hierarchy
```

**السكريبت سيقوم بـ:**
- ✅ إضافة الـ 12 كلية
- ✅ إضافة الأقسام لكل كلية  
- ✅ إضافة المختبرات لكل قسم

**الناتج المتوقع:**
```
📝 Attempting to seed hierarchy...
DB Host: localhost
DB Name: bua_assets
✅ Database connected
🌱 Seeding faculties, departments, and laboratories...

✨ Seeding complete!
```

---

## 📊 البيانات المضافة

### الكليات (12 كلية):
```
1. كلية الصيدلة (PHARM)
2. كلية طب الفم والأسنان (DENT)
3. كلية الطب البيطري (VET)
4. كلية التمريض (NURS)
5. كلية العلاج الطبيعي (PT)
6. كلية العلوم الصحية (HS)
7. كلية البيوتكنولوجيا (BIO)
8. كلية الطب البشري (MED)
9. كلية الهندسة (ENG)
10. كلية العلوم (SCI)
11. كلية الآداب (ART)
12. كلية التربية (EDU)
```

### كل كلية تحتوي على:
- **2-3 أقسام** (Department)
- **كل قسم يحتوي على مختبر واحد أو أكثر** (Laboratory)

---

## 🚀 اختبر الحل

بعد تشغيل سكريبت البذر:

1. افتح التطبيق: `npm run dev`
2. انتقل إلى صفحة إضافة جهاز جديد: `/devices/new`
3. يجب أن ترى:
   - ✅ الـ 12 كلية في قائمة الكليات
   - ✅ الأقسام تظهر بعد اختيار كلية
   - ✅ المختبرات تظهر بعد اختيار قسم

---

## 🛠️ استكشاف الأخطاء

### الخطأ: `DB_NAME is undefined`
**السبب:** الملف `.env` لم يتم قراءته  
**الحل:** تأكد من:
- ✓ اسم الملف صحيح: `.env` (بدون امتداد)
- ✓ الملف في جذر المشروع (بجانب `package.json`)
- ✓ أعد تشغيل الـ terminal بعد إنشاء الملف

### الخطأ: `ECONNREFUSED`
**السبب:** خادم MySQL لا يستجيب  
**الحل:** تأكد من:
- ✓ خدمة MySQL تعمل
- ✓ قيم الاتصال صحيحة (HOST, PORT, USER, PASSWORD)
- ✓ اسم قاعدة البيانات موجود

### الخطأ: `Unknown database`
**السبب:** قاعدة البيانات لم تُنشأ  
**الحل:** أنشئ قاعدة البيانات يدويًا:

```sql
CREATE DATABASE bua_assets CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

---

## 📝 معلومات إضافية

### ملفات البذر المتوفرة:
- `seed-hierarchy.ts` - البيانات الأساسية (جديد) ✅
- `seed-faculties-v2.ts` - فقط الكليات
- `seed-dev-data.ts` - بيانات التطوير

### أوامر مفيدة:
```bash
# عرض البيانات المدرجة
mysql -h localhost -u root -p bua_assets -e "SELECT COUNT(*) as faculties FROM faculties; SELECT COUNT(*) as departments FROM departments; SELECT COUNT(*) as laboratories FROM laboratories;"

# حذف البيانات وإعادة تشغيل السكريبت
npm run seed:hierarchy
```

---

## ✅ تم حل المشكلة!

بعد اتباع هذه الخطوات:
- ✅ ستظهر الـ 12 كلية
- ✅ ستظهر الأقسام والمختبرات
- ✅ ستتمكن من إضافة أجهزة بنجاح
- ✅ سيتم تعيين الأجهزة للكليات والأقسام بشكل صحيح
